{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -d backend ]; then cd backend; fi; npm ci"
  },
  "deploy": {
    "startCommand": "if [ -d backend ]; then cd backend; fi; npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120
  }
}
