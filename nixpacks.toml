[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["if [ -d backend ]; then cd backend; fi; npm ci"]

[phases.build]
cmds = ["echo No build step for backend"]

[start]
cmd = "if [ -d backend ]; then cd backend; fi; npm start"
